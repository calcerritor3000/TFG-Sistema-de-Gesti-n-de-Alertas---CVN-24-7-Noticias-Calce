# Copia a .env.local para desarrollo

# Vacío = mismo origen que el frontend (recomendado en producción si sirves React desde Express)
REACT_APP_API_URL=http://localhost:4000

# REACT_APP_ALERT_RADIUS_MAX_METERS=25000
