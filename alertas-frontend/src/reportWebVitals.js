// web-vitals eliminado para reducir dependencias
const reportWebVitals = onPerfEntry => {
  // Función deshabilitada - web-vitals eliminado
  if (onPerfEntry && onPerfEntry instanceof Function) {
    // No hacer nada - librería eliminada
  }
};

export default reportWebVitals;
